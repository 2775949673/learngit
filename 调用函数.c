#include <stdio.h>
void main() {
	/*int x,y;
	scanf("%d%d",&x&y);

	if(x>y)
		max=y;
	else
		max=y;

	printf("max=%d\n",max);*/

	int x,y,max;
	scanf("%d%d",&x,&y);
	max=max2(x,y);
	printf("max=%d\n¡±,max);
	 }      

	       int max2(in a,int b){
	       if(a>b)
	       max=a;
	       else
	       max=b;
	       return max;
       }

